<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:controls="clr-namespace:CombatHelper.Controls"
             mc:Ignorable="d"
             x:Class="CombatHelper.Controls.Encounter.QuickView">
    <StackLayout>
    <StackLayout Padding="30">
        <Label Text="{Binding Name}" FontSize="Title" TextColor="Black"/>
        <StackLayout Orientation="Horizontal">
            <Image Source="ic_ac"
                   WidthRequest="20"
                   HorizontalOptions="End"/>
            <Button Text="{Binding AC}"
                    Grid.Column="1"
                    BackgroundColor="Transparent"
                    WidthRequest="40"
                    HorizontalOptions="Start"/>
            <Image Source="ic_hp"
                   WidthRequest="20"/>
            <Button Text="{Binding HP}"
                    BackgroundColor="Transparent"
                    WidthRequest="40"/>
        </StackLayout>
        <controls:SpeedControl Grid.Row="2" />
        <controls:AttributeTable Grid.Row="3"/>
        <Label Grid.Row="4" Text="{Binding Info}"/>
    </StackLayout>
    </StackLayout>
</ContentView>